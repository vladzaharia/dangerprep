# =============================================================================
# ARCANE ENVIRONMENT VARIABLES - COMPREHENSIVE EXAMPLE
# =============================================================================
# Complete configuration options for Arcane Docker Management UI
# Reference: https://arcane.ofkm.dev

# -----------------------------------------------------------------------------
# System Configuration
# -----------------------------------------------------------------------------
# Installation Root Path
INSTALL_ROOT=${DANGERPREP_INSTALL_ROOT:-/opt/dangerprep}

# Timezone Configuration
TZ=America/Los_Angeles

# User/Group IDs for file permissions
PUID=1000
PGID=1000

# -----------------------------------------------------------------------------
# Arcane Configuration
# -----------------------------------------------------------------------------
# Application Environment (production, development)
APP_ENV=production

# Session Secret (REQUIRED - Generate with: openssl rand -base64 32)
# This is used for session encryption and MUST be changed from default
PUBLIC_SESSION_SECRET=REPLACE_WITH_SECURE_32_CHARACTER_STRING

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# Allow insecure cookies (only for local HTTP testing without SSL/TLS)
# Should be false in production with HTTPS
PUBLIC_ALLOW_INSECURE_COOKIES=false

# Node.js Environment
NODE_ENV=production

# -----------------------------------------------------------------------------
# Docker Configuration
# -----------------------------------------------------------------------------
# Docker Group ID (usually auto-detected from mounted socket)
# Only set if auto-detection fails
# DOCKER_GID=998

# -----------------------------------------------------------------------------
# Advanced Configuration
# -----------------------------------------------------------------------------
# Enable debug logging
# DEBUG=false

# Custom data directory inside container (default: /app/data)
# DATA_DIR=/app/data

# Custom port (default: 3000)
# PORT=3000

# =============================================================================
# WISHLIST SSH FRONTDOOR ENVIRONMENT VARIABLES
# =============================================================================
# Configuration for Wishlist SSH directory and frontdoor service
# Wishlist provides a menu-driven SSH interface on port 3222 that connects to
# the main SSH server on port 22 and future management applications
# Reference: https://github.com/charmbracelet/wishlist

# -----------------------------------------------------------------------------
# System Configuration
# -----------------------------------------------------------------------------
# Installation Root Path
INSTALL_ROOT=${DANGERPREP_INSTALL_ROOT:-/opt/dangerprep}

# Timezone Configuration
TZ={{TZ}}

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------
# The port that the internal SSH server runs on (Wishlist will connect to this)
# This should match the SSH_PORT configured in the main setup
X_SSH_INTERNAL_PORT={{SSH_PORT}}

# -----------------------------------------------------------------------------
# User Configuration
# -----------------------------------------------------------------------------
# Primary user account (configured during setup)
X_PRIMARY_USER={{NEW_USERNAME}}

# GitHub username for SSH key import (configured during setup)
X_GITHUB_USERNAME={{GITHUB_USERNAME}}

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# External port for Wishlist SSH frontdoor (host port mapping)
WISHLIST_PORT={{WISHLIST_PORT}}

# Wishlist listens on all interfaces within the container
X_LISTEN_ADDRESS=0.0.0.0

# Port for Wishlist to listen on (should be 22 for SSH frontdoor)
X_LISTEN_PORT=22

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# Whether to enable SSH agent forwarding for connections
X_ENABLE_AGENT_FORWARDING=true

# Connection timeout for SSH connections (in seconds)
X_CONNECTION_TIMEOUT=30

# Maximum number of concurrent SSH connections
X_MAX_CONNECTIONS=100

# Request TTY for interactive sessions
X_REQUEST_TTY=true

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level for Wishlist (debug, info, warn, error)
X_LOG_LEVEL=info

# Enable access logging
X_ACCESS_LOG=true

# -----------------------------------------------------------------------------
# Metrics Configuration
# -----------------------------------------------------------------------------
# Enable Prometheus metrics endpoint
X_METRICS_ENABLED=false

# Metrics endpoint address
X_METRICS_ADDRESS=localhost:9223

# Metrics service name
X_METRICS_NAME=dangerprep-wishlist

# =============================================================================
# DANGERPREP PORTAL CONFIGURATION
# =============================================================================
# This file contains environment variables for the DangerPrep Portal service.
# The portal provides a modern web interface for accessing hotspot services
# and WiFi connection information via QR codes.
#
# The portal uses RUNTIME CONFIGURATION - environment variables are loaded
# dynamically via the /api/config endpoint, allowing updates without rebuilds.
#
# Copy this file to compose.env and customize the values for your setup.
# =============================================================================

# -----------------------------------------------------------------------------
# System Configuration
# -----------------------------------------------------------------------------
# Installation Root Path
INSTALL_ROOT=${DANGERPREP_INSTALL_ROOT:-/opt/dangerprep}

# Timezone Configuration
TZ={{TZ}}

# -----------------------------------------------------------------------------
# WiFi Hotspot Configuration (Runtime)
# -----------------------------------------------------------------------------
# The portal automatically reads WiFi configuration from the system's hostapd
# configuration (/etc/hostapd/hostapd.conf). These values serve as fallbacks
# if the system configuration cannot be read.

# WiFi network name - fallback value if hostapd.conf cannot be read
WIFI_SSID={{WIFI_SSID}}

# PROMPT[password]: WiFi network password - fallback value if hostapd.conf cannot be read
WIFI_PASSWORD=your_wifi_password_here

# -----------------------------------------------------------------------------
# Service URLs Configuration (Runtime)
# -----------------------------------------------------------------------------
# These values are loaded dynamically by the frontend via /api/config
# Changes take effect immediately without rebuilding the application

# Base domain for constructing service URLs
# This will be combined with service subdomains to create full URLs
BASE_DOMAIN=danger.diy

# Service subdomains - these will be combined with BASE_DOMAIN
# Example: JELLYFIN_SUBDOMAIN=media + BASE_DOMAIN=argos.surf = https://media.argos.surf

# Main services
JELLYFIN_SUBDOMAIN=media
KIWIX_SUBDOMAIN=kiwix
ROMM_SUBDOMAIN=retro

# Maintenance services
DOCMOST_SUBDOMAIN=docmost
ONEDEV_SUBDOMAIN=onedev
TRAEFIK_SUBDOMAIN=traefik
KOMODO_SUBDOMAIN=docker

# -----------------------------------------------------------------------------
# Portal Application Configuration
# -----------------------------------------------------------------------------
# Node.js Environment (production for optimized builds)
NODE_ENV=production

# Build-time configuration
VITE_APP_TITLE=DangerPrep Portal
VITE_APP_DESCRIPTION=Your portable hotspot services portal

# -----------------------------------------------------------------------------
# FontAwesome Pro Kit Configuration
# -----------------------------------------------------------------------------
# Auth token for FontAwesome Pro kit package (@awesome.me/kit-a765fc5647)
# This token is required in .yarnrc.yml to authenticate with npm.fontawesome.com
# Replace with your actual FontAwesome npm auth token
FONTAWESOME_NPM_AUTH_TOKEN=your_fontawesome_token_here

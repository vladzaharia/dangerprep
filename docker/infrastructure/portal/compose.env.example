# =============================================================================
# DANGERPREP PORTAL CONFIGURATION
# =============================================================================
# This file contains environment variables for the DangerPrep Portal service.
# The portal provides a modern web interface for accessing hotspot services
# and WiFi connection information via QR codes.
#
# Copy this file to compose.env and customize the values for your setup.
# =============================================================================

# -----------------------------------------------------------------------------
# System Configuration
# -----------------------------------------------------------------------------
# Installation Root Path
INSTALL_ROOT=${DANGERPREP_INSTALL_ROOT:-/opt/dangerprep}

# Timezone Configuration
TZ={{TZ}}

# -----------------------------------------------------------------------------
# WiFi Hotspot Configuration
# -----------------------------------------------------------------------------
# This will be displayed in the portal and encoded in the QR code
WIFI_SSID={{WIFI_SSID}}

# PROMPT[password]: WiFi network password for the hotspot
# This will be displayed in the portal and encoded in the QR code
WIFI_PASSWORD=your_wifi_password_here

# -----------------------------------------------------------------------------
# Service URLs Configuration
# -----------------------------------------------------------------------------
# Base domain for constructing service URLs
# This will be combined with service subdomains to create full URLs
BASE_DOMAIN=danger.diy

# Service subdomains - these will be combined with BASE_DOMAIN
# Example: JELLYFIN_SUBDOMAIN=media + BASE_DOMAIN=argos.surf = https://media.argos.surf

# Main services
JELLYFIN_SUBDOMAIN=media
KIWIX_SUBDOMAIN=kiwix
ROMM_SUBDOMAIN=retro

# Maintenance services
DOCMOST_SUBDOMAIN=docmost
ONEDEV_SUBDOMAIN=onedev
TRAEFIK_SUBDOMAIN=traefik
PORTAINER_SUBDOMAIN=portainer

# -----------------------------------------------------------------------------
# Portal Application Configuration
# -----------------------------------------------------------------------------
# Node.js Environment (production for optimized builds)
NODE_ENV=production

# Build-time configuration
VITE_APP_TITLE=DangerPrep Portal
VITE_APP_DESCRIPTION=Your portable hotspot services portal

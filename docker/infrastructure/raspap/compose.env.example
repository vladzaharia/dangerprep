# =============================================================================
# RASPAP ENVIRONMENT VARIABLES - DANGERPREP INTEGRATION
# =============================================================================
# Configuration for RaspAP Docker container with DangerPrep integration
# Copy this file to compose.env and customize as needed

# -----------------------------------------------------------------------------
# System Configuration
# -----------------------------------------------------------------------------
# Installation Root Path
INSTALL_ROOT=${DANGERPREP_INSTALL_ROOT:-/opt/dangerprep}

# Timezone Configuration
TZ=America/Los_Angeles

# User/Group IDs (RaspAP runs as root for network management)
PUID=0
PGID=0

# -----------------------------------------------------------------------------
# RaspAP Basic Configuration
# -----------------------------------------------------------------------------
# WiFi Access Point Settings
RASPAP_SSID=DangerPrep
RASPAP_SSID_PASS=EXAMPLE_PASSWORD
RASPAP_COUNTRY=US

# Web Interface Settings
RASPAP_WEBGUI_USER=admin
RASPAP_WEBGUI_PASS=secret
RASPAP_WEBGUI_PORT=80

# -----------------------------------------------------------------------------
# Network Interface Configuration
# -----------------------------------------------------------------------------
# Network Interfaces (auto-detected if not specified)
RASPAP_WIFI_INTERFACE=wlan0
RASPAP_ETH_INTERFACE=eth0

# IP Address Configuration
RASPAP_IP_ADDRESS=192.168.120.1
RASPAP_NETMASK=255.255.252.0

# DHCP Configuration
RASPAP_DHCP_START=192.168.120.100
RASPAP_DHCP_END=192.168.120.200
RASPAP_DHCP_LEASE=24h

# -----------------------------------------------------------------------------
# DNS Configuration for DangerPrep Integration
# -----------------------------------------------------------------------------
# Primary DNS (RaspAP will forward .danger domains to CoreDNS on port 5353)
RASPAP_DNS_PRIMARY=127.0.0.1

# Secondary DNS (fallback)
RASPAP_DNS_SECONDARY=1.1.1.1

# DNS Forwarding Rules (configured via RaspAP web interface)
# .danger domains -> 127.0.0.1:5353 (CoreDNS)
# Other domains -> 127.0.0.1:3000 (AdGuard web interface port, or use Docker IP 172.20.0.2:53)

# -----------------------------------------------------------------------------
# RaspAP Insiders Configuration
# -----------------------------------------------------------------------------
# Enable Insiders features
RASPAP_INSIDERS=true

# GitHub Credentials for Insiders Access
# PROMPT: GitHub username for RaspAP Insiders access
GITHUB_USERNAME=your_github_username

# PASSWORD: GitHub personal access token for RaspAP Insiders access
GITHUB_TOKEN=your_github_token

# -----------------------------------------------------------------------------
# Advanced Features (Insiders)
# -----------------------------------------------------------------------------
# Enable built-in firewall management
RASPAP_ENABLE_FIREWALL=true

# Enable Tailscale VPN integration
RASPAP_ENABLE_TAILSCALE=true

# Enable network diagnostics tools
RASPAP_ENABLE_DIAGNOSTICS=true

# Enable WiFi repeater mode (requires compatible hardware)
RASPAP_ENABLE_REPEATER=false

# Enable WPA3 security (if supported by hardware)
RASPAP_ENABLE_WPA3=true

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level (debug, info, warn, error)
RASPAP_LOG_LEVEL=info

# Enable access logging
RASPAP_ACCESS_LOG=true

# Enable error logging
RASPAP_ERROR_LOG=true

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# Enable HTTPS for web interface (requires SSL certificates)
RASPAP_ENABLE_HTTPS=false

# SSL Certificate paths (if HTTPS enabled)
RASPAP_SSL_CERT=/etc/ssl/certs/raspap.crt
RASPAP_SSL_KEY=/etc/ssl/private/raspap.key

# Enable user access control
RASPAP_ENABLE_USER_CONTROL=true

# Session timeout (minutes)
RASPAP_SESSION_TIMEOUT=30

# -----------------------------------------------------------------------------
# Performance Configuration
# -----------------------------------------------------------------------------
# WiFi Channel (auto-select if 0)
RASPAP_WIFI_CHANNEL=0

# WiFi Bandwidth (20, 40, 80, 160 MHz)
RASPAP_WIFI_BANDWIDTH=80

# Maximum WiFi clients
RASPAP_MAX_CLIENTS=50

# Enable QoS traffic shaping
RASPAP_ENABLE_QOS=false

# -----------------------------------------------------------------------------
# Integration Settings
# -----------------------------------------------------------------------------
# Docker socket access for container management
DOCKER_SOCKET=/var/run/docker.sock

# Enable Docker integration features
RASPAP_DOCKER_INTEGRATION=true

# Enable system monitoring
RASPAP_ENABLE_MONITORING=true

# Update check interval (hours, 0 to disable)
RASPAP_UPDATE_CHECK_INTERVAL=24
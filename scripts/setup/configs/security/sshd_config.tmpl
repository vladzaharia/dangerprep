# DangerPrep SSH Configuration - Basic Setup
# SSH running on default port for easy access during setup
Port {{SSH_PORT}}

# Basic authentication settings
PubkeyAuthentication yes
PasswordAuthentication yes
PermitEmptyPasswords no
UsePAM yes

# Allow root login for initial setup (can be changed later)
PermitRootLogin yes

# Basic security settings
X11Forwarding yes
PrintMotd yes
PrintLastLog yes

# Allow all users by default
# AllowUsers can be configured later for specific restrictions
